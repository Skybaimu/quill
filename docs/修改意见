文件名改名和内容块的标题改名感觉操作还是不方便， 
而且改名标题居然还有弹窗， 

md类别，新建文件夹卡死， 



第二次修改意见， 文件类，文件新建 ，在文件列表名下面加个加号新建  这样更方便  ，上面的加号也别去，
安装的时候没选项比如md格式默认打开， 而我md设置了以quill打开后， 点击md， 
会重新打开一个窗口， 但是md还是没打开。 
文件列表那名称下面有一段文字，是内容的预览， 但是这里感觉不需要， 还导致列表高度太高， 可以去掉
左侧的工作空间改为类别， 而且右边的加号和导入，都太小了。 大一些。 
中间文件列表的选中，显示不明显， 可以加深
右侧的内容区， 内容块的添加按钮， 当内容块太多时候， 也不会把这个内容块推到最下面看不到， 也就是最低也是在底部固定，没内容块或者内容块不多的时候是贴着最后一个内容块的， 但是哪怕是内容很多展开状态， 这个添加内容块按钮，也得在窗口内最底部可以看得到。 